apiVersion: apps/v1
kind: Deployment
metadata:
  name: compra-postgres
spec:
  selector:
    matchLabels:
      app: compra-postgres
  template:
    metadata:
      labels:
        app: compra-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          env:
            - name: POSTGRES_DB
              value: compra_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: compra-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: compra-postgres