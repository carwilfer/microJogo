apiVersion: apps/v1
kind: Deployment
metadata:
  name: conta-rabbitmq
spec:
  selector:
    matchLabels:
      app: conta-rabbitmq
  template:
    metadata:
      labels:
        app: conta-rabbitmq
    spec:
      containers:
        - name: rabbitmq
          image: rabbitmq:3-management
          ports:
            - containerPort: 5672
            - containerPort: 15672
---
apiVersion: v1
kind: Service
metadata:
  name: conta-rabbitmq
spec:
  ports:
    - port: 5672
    - port: 15672
  selector:
    app: conta-rabbitmq