apiVersion: apps/v1
kind: Deployment
metadata:
  name: avaliacao-postgres
spec:
  selector:
    matchLabels:
      app: avaliacao-postgres
  template:
    metadata:
      labels:
        app: avaliacao-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          env:
            - name: POSTGRES_DB
              value: avaliacao_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: avaliacao-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: avaliacao-postgres