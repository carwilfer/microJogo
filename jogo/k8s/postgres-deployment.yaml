apiVersion: apps/v1
kind: Deployment
metadata:
  name: jogo-postgres
spec:
  selector:
    matchLabels:
      app: jogo-postgres
  template:
    metadata:
      labels:
        app: jogo-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          env:
            - name: POSTGRES_DB
              value: jogo_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: jogo-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: jogo-postgres