apiVersion: apps/v1
kind: Deployment
metadata:
  name: jogador-postgres
spec:
  selector:
    matchLabels:
      app: jogador-postgres
  template:
    metadata:
      labels:
        app: jogador-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:14
          env:
            - name: POSTGRES_DB
              value: jogador_db
            - name: POSTGRES_USER
              value: postgres
            - name: POSTGRES_PASSWORD
              value: postgres
          ports:
            - containerPort: 5432
---
apiVersion: v1
kind: Service
metadata:
  name: jogador-postgres
spec:
  ports:
    - port: 5432
  selector:
    app: jogador-postgres